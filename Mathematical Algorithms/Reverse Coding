//Easy
class Solution {
    static int sumOfNaturals(int n) {
        // code here
        long sum = 0;
        for(int i=1;i<=n;i++){
            sum += i;
        }
        return (int)(sum % 1000000007);
    }
}
